# Root-level defaults
NODE_ENV=development

# API
API_HOST=0.0.0.0
API_PORT=3000
DATABASE_URL="file:./dev.db"

# Web (Vite)
WEB_HOST=0.0.0.0
WEB_PORT=5173

# JWT (dev defaults provided if unset)
JWT_ACCESS_SECRET=change-me-access
JWT_REFRESH_SECRET=change-me-refresh
JWT_RESET_SECRET=change-me-reset
